

magick mogrify -path ./compressed -colors 16 -depth 8 -quality 95 -strip *.png
mogrify -fuzz 10% -transparent white *.png






REMOTE_URL=$(git remote get-url origin | sed 's/\.git$//')
BRANCH=$(git branch --show-current)

git ls-files | xargs -I {} echo "${REMOTE_URL}/blob/${BRANCH}/{}"
